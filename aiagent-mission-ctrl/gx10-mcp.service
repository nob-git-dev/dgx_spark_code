[Unit]
Description=GX10 MCP Server
After=network.target

[Service]
Type=simple
WorkingDirectory=%h/projects/gx10-mcp
ExecStart=%h/.local/bin/uv run uvicorn server:app --host 0.0.0.0 --port 9100
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
